id: exposed-git-config
info:
  name: "Git 配置暴露 (.git/config 可公开访问)"
  severity: medium
  tags: "exposure,git"
requests:
  - method: GET
    path: "/.git/config"
    matchers:
      - type: status
        status: 200
      - type: word
        words:
          - "[core]"
        condition: and
